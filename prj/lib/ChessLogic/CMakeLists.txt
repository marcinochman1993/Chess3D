file(GLOB ChessLogic_SRCS ${CMAKE_CURRENT_SOURCE_DIR}/src/*.cpp)
include_directories(inc)
if(UNIX)
  add_definitions(-std=c++11 -pedantic -Wall)
endif(UNIX)
add_library(ChessLogic ${ChessLogic_SRCS})
add_subdirectory(test)